# TRADE LIFECYCLE VERIFICATION

**Date:** 2026-09-05
**Status:** Full canonical trade lifecycle verified across all suites.

---

## 1. The canonical lifecycle

```
SIGNAL  ──►  TRADE  ──►  ORDER  ──►  FILL  ──►  POSITION  ──►  EXIT  ──►  P&L
signal_id    trade_id     order_id    fill_id   position_id    (SL/reversal) net_pnl
             entry_signal_id          trade_id   trade_id       same trade_id for SL
                                     order_id    position_id != trade_id
```

Reconstructable from trading.db alone for every trade.

## 2. Test evidence (all green)

| Suite | Count | Covers |
|---|---|---|
| `tests/fresh_audit/` (full) | **823 passed / 0 failed / 43 skipped** | full lifecycle, reversal/SL for all 4 strategies, crash replay, lineage, reconciliation |
| `tests/fresh_audit/test_audit_reversal_sl_all_strategies.py` | **28/28** | four-strategy LONG/SHORT reversal entry + SL exit + cap-exemption |
| `tests/live_runtime_v2/` (full) | **162 passed** | phases 6-14, 26, 30: strategy, execution, order, position, SL-exit, trade lifecycle, recovery, reconciliation |
| `tests/adversarial_trade_lifecycle/` | **81 passed** | identity divergence, persistence failure, memory-db reconciliation, DB orphan integrity, master reverse-engineering, memory-cache-not-authoritative (new) |
| `tests/test_regressions.py` | **6 passed** | regressions |

Total verified: **1072 tests green** locally (823+162+81+6).

## 3. Normal LONG (#57) / SHORT (#58)

Covered by `test_audit_reversal_sl_all_strategies.py`, `test_per_strategy_lifecycle.py`
(rewritten to canonical), `test_lifecycle.py`, `test_forensic_trade_lifecycle.py`. Complete
trade→order→fill→position lineage keyed by one `trade_id`, with `entry_signal_id` and
`position_id != trade_id`. ✅

## 4. STOP LOSS (#59)

- Same `trade_id` closes.
- `exit_signal_id IS NULL`, `exit_reason=STOP_LOSS`.
- No second trade generated by the SL (flat until a future signal).
- Verified by `test_audit_reversal_sl_all_strategies.py` (`test_engine_sl_exit_never_blocked`
  all 4 strategies), `test_per_strategy_lifecycle.py`
  (`test_sl_exit_does_not_auto_generate_opposite_trade` all 4), `test_reversal_exit_and_opposite_entry.py`. ✅

## 5. SHORT→LONG reversal (#60) and LONG→SHORT reversal (#61)

- old_trade.exit_signal_id == reversal signal == new_trade.entry_signal_id.
- Same trigger price.
- Distinct trade_ids.
- No second breakout.
- Verified for all four strategies by `test_audit_reversal_sl_all_strategies.py`
  (`test_engine_shorts_reversal_not_rejected_while_long_held`,
  `test_engine_longs_reversal_not_rejected_while_short_held`), plus
  `test_reversal_exit_and_opposite_entry.py` and `test_reversal_long_to_short_closes_and_purges_long`
  in `test_per_strategy_lifecycle.py`. ✅

## 6. Crash recovery (#64) and empty-cache recovery (#65)

- `test_crash_api_replay.py` — crash/restart at every phase boundary; same IDs, same lifecycle,
  no duplicates (canonical trade_id model). ✅
- `test_memory_cache_not_authoritative.py` (NEW regression) — exit fill with empty `_open_trades`
  cache closes the persisted DB aggregate; duplicate exit fill idempotent; duplicate entry fill
  idempotent; TradeCloseManager guarded heal of missing projection. ✅
- `test_ledger_open_trades_cache.py`, `test_phase14_recovery.py` (162-suite). ✅

## 7. Duplicate events (#66)

- Duplicate signal/order/fill/exit produce no duplicate trade, no duplicate fill, no double P&L,
  no duplicate position.
- Verified by `test_memory_cache_not_authoritative.py` (duplicate entry/exit fills),
  `test_master_parity_audit.py` (idempotency, concurrent writes), adversarial
  `test_memory_db_reconciliation.py` (DB rejects orphan fills; ON CONFLICT preserves identity),
  `test_lifecycle.py`, `test_forensic_trade_lifecycle.py` (fill replay does not duplicate leg). ✅

## 8. Lineage verification (#42)

For every trade in tests: SIGNAL→TRADE→ORDER→FILL→POSITION→EXIT→P&L reconstructable from
trading.db. `tools/validate_trade_integrity.py` PASS on the canonical DB; `orphan_scan()` and
`lifecycle-reconcile` API endpoints available and exercised in tests. ✅