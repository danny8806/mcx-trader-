# REPLAY FORENSIC BASELINE

Phase 0 — Baseline context for the FULL MCX LIVE ARCHITECTURE FORENSIC REPLAY.

## 1. Window

- **Start**: 2026-09-02 09:00 IST (first bar start)
- **End**: 2026-09-04 23:25:00 (latest closed candle in the offline dataset)
- **Bar counts** (per 5m grid, all three feeds): 522 × GOLDM 5m, 522 × SILVERM 5m, plus native 15m and 60m feeds for the HTF lines. The evaluation stream records 522 bar-starts per strategy.

## 2. Data source

- **Type**: offline deterministic CSVs under `replay_input/` (GOLDM/SILVERM 5m/15m/60m), generated by `%TEMP%\opencode\gen_replay_csvs.py` with deterministic seed `20260902`.
- **Why offline**: online historical replay is blocked on this host by `DhanAuthError DH-901` (token expired/invalid). The offline CSVs are treated as the window's authoritative market data. `boundary_checks.csv` (reference) marks `data_source = offline_csv`.
- Each CSV row: `datetime` (open time), `open, high, low, close, volume`, `close_time` derived (start+tf).

## 3. Engine stack under test (LIVE architecture)

- `TradingEngine` (`trading_engine.py`) — real production engine.
- `NativeCandleRouter` (native instrument→candle dispatch) and the shared multi-strategy indicator engine.
- 4 production strategies via `STRATEGY_FACTORIES` (authoritative):
  | mission id | instrument | config | base / mid / slow |
  |---|---|---|---|
  | `gold_01` | GOLDM (569003) | `create_gold_5m` | 5m / 15m / 1h |
  | `gold_02` | GOLDM (569003) | `create_gold_15m` | 15m / 15m / 1h |
  | `silver_01` | SILVERM (483080) | `create_silver_15m` | 15m / 15m / 1h |
  | `silver_02` | SILVERM (483080) | `create_silver_5m` | 5m / 15m / 1h |
- Indicator engine: DEMA-3 / ATR-6 / factor 1.0 mapped HTF lines (shared, mode-parallel and isolation-safe).
- Execution: paper execution engine; everything persisted to the canonical `signals / trades / orders / fills / positions / trade_events / trade_legs / broker_order_mapping / …` schema in a fresh per-run SQLite DB.
- Time: execution clock set to the replay clock (bar end); bars are fed chronologically, ranked 1h→15m→5m at equal `end_ts`.

## 4. Reference (oracle)

- `C:\Users\pc\Desktop\nifty dema backtest\project\core\dema_mtf.py` — NEVER modified.
- Driven by `tools/replay_mcx_from_2026_09_02.py` (which WAS fixed this session: its silver base mapping was inverted; corrected to silver_01 base=15, silver_02 base=5 to match the repo).
- Reference emits per-bar boundary `h15`/`h1` + signals (51) + trades (28) over the same window.

## 5. Determinism

- Dataset: fixed seed ⇒ identical OHLC series. Live replay fed the same bars in the same ranked order in every run.
- **Phase 37 evidence**: two independent parallel runs produced SHA-256-identical normalized streams — in-memory signals, trades, crossover events, and the 522-bar evaluation stream all matched (hashes in `forensics/phase67_checksums.json`).

## 6. Artifact inventory (committed under `replay_output/`)

- `live_replay/parallel/` — full/signal/persisted_signal/trade/order/fill/position/pnl/strategy/crossover JSON, `db_integrity.json`, `evaluation_stream.jsonl`, `checksums.json`.
- `live_replay/sequential/` — per-strategy `{sid}.json` (signals/trades/crossovers/strategies) + `sequential.json`.
- `live_replay/reconciliation/` — signal parity, causes, trade-lineage, parallel-vs-sequential, `reconciliation.json`.
- `live_replay/forensics/` — signal/trade forensics, reversal invariant, phase 67 checksums, indicator parity, legacy scan.
- `replay_2026-09-02_to_latest/` — regenerated reference artifacts (boundary checks, per-strategy signals/trades, all_signals/all_trades CSVs, summary).